# Basid CRUD using NodeJS, KnexJS and ObjectionJS

## Installation

- Install `Knex` CLI using `npm install knex -g` command.
- Run `npm install`
- Run `npm run migrate`
- Run `npm start`

## DB Config file
Location: ***src/config/knexfile.ts***
